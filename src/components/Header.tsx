import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="header">
      <div className="container">
        <h1>AI Chatbot Widget Builder</h1>
        <p>Create your custom AI chatbot in minutes</p>
      </div>
    </header>
  );
};

export default Header;
